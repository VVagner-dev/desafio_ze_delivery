spring:
  application:
    name: desafioze

  datasource:
    url: jdbc:postgresql://localhost:5432/ze_delivery
    username: wagner
    password: "123"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.spatial.dialect.postgis.PostgisDialect
        format_sql: true